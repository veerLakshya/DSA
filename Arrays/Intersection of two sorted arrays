#include <iostream>
#include <vector>
using namespace std;

int intersectionArray(int arr[], int brr[], int n, int m)
{
	int i=0,j=0;
	vector <int> temp;

	while(i<n && j<m)
	{
		if(arr[i]<brr[j]) i++;
		else if(brr[j]<arr[i]) j++;
		else
		{
			temp.push_back(arr[i]);
			i++; j++;
		}
	}
	for(int i=0;i<temp.size();i++)
	arr[i] = temp[i];

	return temp.size();
}

int main()
{
	int n;
	cin>>n;
	int a[n];
	for(int i=0;i<n;i++)
	cin>>a[i];

	int m;
	cin>>m;
	int b[m];
	for(int i=0;i<m;i++)
	cin>>b[i];

	int numberOfElements = intersectionArray( a, b, n , m);

	for(int i=0;i<numberOfElements;i++)
	cout<<a[i]<<" ";

}
